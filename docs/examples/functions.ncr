fn greet(name: String, prefix: String = "Hello"): String {
return "${prefix}, ${name}"
}

const defaultGreeting = greet("Nacre")
const customGreeting = greet("Nacre", "Hi")

run.output.echo(defaultGreeting)
run.output.echo(customGreeting)
